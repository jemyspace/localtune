[build]
  base = "app"
  command = "npm ci && npm run build"
  publish = "dist"
  functions = "../netlify/functions"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/api/research"
  to = "/.netlify/functions/research"
  status = 200
